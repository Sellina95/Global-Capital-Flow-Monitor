# Geo Experiments (Backtest / Validation)

This folder is for experimental validation only.
- It must NOT affect production daily pipeline.
- Outputs are written to `insights/geo_backtest.txt` (and optional cached CSV in `data/geo_history.csv`).
