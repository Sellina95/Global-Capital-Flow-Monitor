name: Daily Macro Report

on:
  workflow_dispatch:
  schedule:
    # 매일 UTC 22:00 → KST 오전 7시
    - cron: "0 22 * * *"

permissions:
  contents: write

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      # 1️⃣ 레포 체크아웃
      - name: Checkout repository
        uses: actions/checkout@v4

      # 2️⃣ 파이썬 세팅
      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install dependencies
        run: |
         python -m pip install --upgrade pip
         pip install pandas openpyxl yfinance  # yfinance 추가



      # 4️⃣ 디렉토리 보장
      - name: Ensure required directories exist
        run: |
          mkdir -p data
          mkdir -p reports

      # 🔥 5️⃣ (핵심) 매크로 데이터 수집 & 저장
      - name: Fetch macro data
        run: |
          export PYTHONPATH="$GITHUB_WORKSPACE"
          python scripts/fetch_macro_data.py

      # 6️⃣ 리포트 생성
      - name: Generate daily report
        run: |
          export PYTHONPATH="$GITHUB_WORKSPACE"
          python scripts/generate_report.py

      # 7️⃣ 데이터 + 리포트 자동 커밋
      - name: Commit and push updates
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"

          git add reports/*.md insights/* || true


          if git diff --cached --quiet; then
            echo "No changes to commit"
            exit 0
          fi

          git commit -m "chore: update macro data and daily report"
          git push
